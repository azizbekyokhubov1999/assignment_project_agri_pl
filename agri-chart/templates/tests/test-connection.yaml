apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "agri-chart.fullname" . }}-test-connection"
  labels:
    {{- include "agri-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "agri-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
