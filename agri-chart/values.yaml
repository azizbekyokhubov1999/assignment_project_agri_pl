replicaCount: 1

image:
  repository: azizdevops88/agri-backend
  tag: 1.0.2
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8080

metrics:
  enabled: true
  path: /metrics
  port: 8080

monitoring:
  enabled: true
  path: /metrics